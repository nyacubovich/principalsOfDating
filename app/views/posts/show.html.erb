<%= render "layouts/navbar" %>

<div class="container">
  <div class="jumbotron">
    <h1><%= @post.title %></h1>
    <h4>
      <i>By Miss Wormwood</i>
    </h4>
    <br>
    <p>
      <%= @post.content %>
    </p>
    <% if signed_in? %>
    <%= link_to 'Edit', edit_post_path(@post) %> |
    <% end %>
    <%= link_to 'Back', posts_path %>
  </div>
</div>